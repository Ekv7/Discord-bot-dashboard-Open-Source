<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Existierende Dateien/Ordner normal ausliefern
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # SPA-Fallback fuer React-Routen
  RewriteRule ^ index.html [L]
</IfModule>

